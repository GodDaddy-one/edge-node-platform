<view class="page">
  <view class="card" wx:for="{{faqs}}" wx:key="q">
    <view class="list-item">
      <view style="font-weight:600;">{{item.q}}</view>
      <view class="muted">{{item.a}}</view>
    </view>
  </view>
</view>
